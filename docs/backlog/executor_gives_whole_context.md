## uexecutor gives underlying implementors the full context

The whole design document is provided, with additional clarification about the phase that implementor must deliver.
We do not try to limit information for the implementor, about the whole RFC scope.
Better for him to have a bit excessive information, than to make some unfortunate decisions due to lacking the whole RFC context while working on the code.